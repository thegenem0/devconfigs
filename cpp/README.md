# C++ Config files

- Configs use LLVM style formatting.
- `.clang-tidy` is configured with useful checks for safety and performance.
- Ignore file is configured for third-party libraries in the `vendor` directory.
